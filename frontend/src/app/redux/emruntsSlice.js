import { createSlice } from "@reduxjs/toolkit";

const empruntSlice = createSlice({
    name:'emprunt',
    initialState:{
         

    },
    reducers:{
    }
})
export const  {} = empruntSlice.actions;
export default empruntSlice.reducer;